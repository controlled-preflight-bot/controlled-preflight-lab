name: stage-b-candidate-runtime
on: pull_request
permissions: {}
jobs:
  altered-host-step:
    runs-on: ubuntu-24.04
    steps:
      - name: Candidate-controlled host shell that must never be approved
        shell: bash
        run: |
          set -euo pipefail
          echo 'forbidden candidate host execution'

